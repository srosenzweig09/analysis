#!/bin/sh

#SBATCH --job-name=ranks
#SBATCH --qos=avery
#SBATCH --account=avery
#SBATCH --time=4:00:00
#SBATCH --partition=hpg-dev
#SBATCH --cpus-per-task=1
#SBATCH --mem-per-cpu=8GB
#SBATCH --output=slurm_output/slurm-%A_%a.out

# sbatch scripts/feynnet/ranking/run

random_number=$RANDOM

mkdir -p tmp

cfg="tmp/feynnet_$random_number.cfg"
cp config/feynnet.cfg $cfg
python scripts/feynnet/ranking/print_config.py --config $cfg

sh scripts/feynnet/ranking/submitRanksToSLURM.sh $cfg

python scripts/feynnet/ranking/plot_ranks.py --config $cfg

rm -r $cfg
rm tmp/*$random_number*